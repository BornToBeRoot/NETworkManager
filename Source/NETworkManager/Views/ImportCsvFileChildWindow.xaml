<simpleChildWindow:ChildWindow x:Class="NETworkManager.Views.ImportCsvFileChildWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:validators="clr-namespace:NETworkManager.Validators;assembly=NETworkManager.Validators"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:converters="clr-namespace:NETworkManager.Converters;assembly=NETworkManager.Converters"
             xmlns:viewModels="clr-namespace:NETworkManager.ViewModels"
             xmlns:localization="clr-namespace:NETworkManager.Localization.Resources;assembly=NETworkManager.Localization"
             xmlns:simpleChildWindow="clr-namespace:MahApps.Metro.SimpleChildWindow;assembly=MahApps.Metro.SimpleChildWindow"
             CloseButtonCommand="{Binding CancelCommand}"
             Style="{StaticResource DefaultChildWindow}"
             Loaded="ChildWindow_OnLoaded"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:ImportCsvFileViewModel}">
    <simpleChildWindow:ChildWindow.Resources>
        <converters:BooleanToVisibilityCollapsedConverter x:Key="BooleanToVisibilityCollapsedConverter" />
    </simpleChildWindow:ChildWindow.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.Resources>
                <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CenterTextBlock}" />
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- File path -->
            <TextBlock Grid.Column="0" Grid.Row="0"
                       Text="{x:Static localization:Strings.FilePath}" />
            <TextBox x:Name="TextBoxFilePath" Grid.Column="2" Grid.Row="0"
                     mah:TextBoxHelper.Watermark="{x:Static localization:StaticStrings.CsvImportFileLocationDots}"
                     Style="{StaticResource BrowseFolderTextBox}"
                     mah:TextBoxHelper.ButtonCommand="{Binding BrowseFileCommand}" AllowDrop="True"
                     Drop="TextBoxFilePath_Drop" PreviewDragOver="TextBoxFilePath_PreviewDragOver">
                <TextBox.Text>
                    <Binding Path="FilePath" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:EmptyValidator ValidatesOnTargetUpdated="True" />
                            <validators:FileExistsValidator ValidatesOnTargetUpdated="True" />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <Rectangle Grid.Column="3" Grid.Row="0"
                       Width="24" Height="24"
                       Style="{StaticResource HelpImageRectangle}"
                       Margin="10,0,0,0">
                <Rectangle.ToolTip>
                    <ToolTip Style="{StaticResource DefaultToolTip}" MaxWidth="420">
                        <StackPanel>
                            <TextBlock Text="{x:Static localization:Strings.CsvImportFormatHint}"
                                       Style="{StaticResource DefaultTextBlock}"
                                       TextWrapping="Wrap" Margin="0,0,0,8" />
                            <Border BorderBrush="{DynamicResource MahApps.Brushes.Gray8}"
                                    BorderThickness="1" CornerRadius="2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.Resources>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="Padding" Value="6,3" />
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                        </Style>
                                    </Grid.Resources>

                                    <!-- Header row -->
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" FontWeight="Bold" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Host" FontWeight="Bold" />
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Description" FontWeight="Bold" />
                                    <Rectangle Grid.Row="1" Grid.ColumnSpan="3" Grid.Column="0" Height="1"
                                               Fill="{DynamicResource MahApps.Brushes.Gray8}" />

                                    <!-- Example rows -->
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Webserver" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="192.168.1.10" />
                                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Production web server" />
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="DC01" />
                                    <TextBlock Grid.Row="3" Grid.Column="1" Text="dc01.corp.local" />
                                </Grid>
                            </Border>
                            <TextBlock Text="{x:Static localization:Strings.CsvImportFormatNote}"
                                       Style="{StaticResource DefaultTextBlock}"
                                       TextWrapping="Wrap" Margin="0,8,0,0" />
                        </StackPanel>
                    </ToolTip>
                </Rectangle.ToolTip>
            </Rectangle>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Style="{StaticResource AccentTextBlock}"
                       Visibility="{Binding IsStatusMessageDisplayed, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}"
                       Text="{Binding StatusMessage}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="{x:Static localization:Strings.Parse}" Command="{Binding ParseCommand}"
                        IsDefault="True" Margin="0,0,10,0">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource HighlightedButton}">
                            <Style.Triggers>
                                <DataTrigger
                                    Binding="{Binding ElementName=TextBoxFilePath, Path=(Validation.HasError)}"
                                    Value="True">
                                    <Setter Property="IsEnabled" Value="False" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="{x:Static localization:Strings.Cancel}" Command="{Binding CancelCommand}"
                        IsCancel="True" Style="{StaticResource DefaultButton}" />
            </StackPanel>
        </Grid>
    </Grid>
</simpleChildWindow:ChildWindow>
