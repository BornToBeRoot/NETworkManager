<UserControl x:Class="NETworkManager.Views.SettingsNetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:validators="clr-namespace:NETworkManager.Validators;assembly=NETworkManager.Validators"
             xmlns:localization="clr-namespace:NETworkManager.Localization.Resources;assembly=NETworkManager.Localization"
             xmlns:viewModels="clr-namespace:NETworkManager.ViewModels"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:SettingsNetworkViewModel}">
    <StackPanel>
        <TextBlock Style="{StaticResource HeaderTextBlock}" 
                   Text="{x:Static localization:Strings.DNS}" />
        <mah:ToggleSwitch Header="{x:Static localization:Strings.UseCustomDNSServer}"
                          IsOn="{Binding UseCustomDNSServer}"
                          Margin="0,0,0,10" />
        <TextBlock Text="{x:Static localization:Strings.DNSServers}" 
                   Style="{DynamicResource DefaultTextBlock}"
                   Margin="0,0,0,10" />
            <TextBlock Text="{Binding CustomDNSServersDisplay, Mode=OneWay}"
                       Style="{DynamicResource ConsoleTextBlock}"
                       Margin="0,0,0,10" />
            <Button Command="{Binding EditCustomDNSServersCommand}"
                    Style="{StaticResource ResourceKey=ImageWithTextButton}"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,10">
                <Button.Content>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Rectangle Grid.Column="0" Grid.Row="0"
                                   Style="{StaticResource ResourceKey=ButtonWithImageRectangle}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Uniform" Visual="{iconPacks:Modern Kind=Edit}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <TextBlock Grid.Column="1" Text="{x:Static Member=localization:Strings.EditDNSServer}"
                                   Style="{StaticResource ResourceKey=ButtonWithImageTextBlock}" />
                    </Grid>
                </Button.Content>
            </Button>
        <mah:ToggleSwitch Header="{x:Static localization:Strings.AddDNSSuffixToHostname}"
                          IsOn="{Binding AddDNSSuffix}" Margin="0,0,0,10" />
        <mah:ToggleSwitch Header="{x:Static localization:Strings.UseCustomDNSSuffix}"
                          IsEnabled="{Binding AddDNSSuffix}" IsOn="{Binding UseCustomDNSSuffix}"
                          Margin="0,0,0,10" />
        <TextBox Width="250" HorizontalAlignment="Left"
                 mah:TextBoxHelper.Watermark="{x:Static localization:StaticStrings.ExampleSuffix}" Margin="0,0,0,10">
            <TextBox.Resources>
                <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource DefaultTextBox}">
                    <Setter Property="IsEnabled" Value="False" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding AddDNSSuffix}" Value="True" />
                                <Condition Binding="{Binding UseCustomDNSSuffix}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter Property="IsEnabled" Value="True" />
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Resources>
            <TextBox.Text>
                <Binding Path="CustomDNSSuffix" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <validators:DNSSuffixValidator ValidatesOnTargetUpdated="True" />
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <TextBlock Style="{StaticResource CenterTextBlock}"
                   Text="{x:Static localization:Strings.PreferredProtocolWhenResolvingHostname}" TextWrapping="Wrap"
                   VerticalAlignment="Top" Margin="0,0,0,10" />
        <RadioButton Content="{x:Static localization:Strings.IPv4}" IsChecked="{Binding ResolveHostnamePreferIPv4}"
                     Margin="0,0,0,10" />
        <RadioButton Content="{x:Static localization:Strings.IPv6}" IsChecked="{Binding ResolveHostnamePreferIPv6}" />
        <!--
        <TextBlock Style="{StaticResource HeaderTextBlock}" Text="{x:Static localization:Strings.Proxy}" />
        -->
    </StackPanel>
</UserControl>
