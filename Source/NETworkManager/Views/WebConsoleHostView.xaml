<UserControl x:Class="NETworkManager.Views.WebConsoleHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
             xmlns:dockablz="clr-namespace:Dragablz.Dockablz;assembly=Dragablz"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:converters="clr-namespace:NETworkManager.Converters;assembly=NETworkManager.Converters"
             xmlns:controls="clr-namespace:NETworkManager.Controls;assembly=NETworkManager.Controls"
             xmlns:viewModels="clr-namespace:NETworkManager.ViewModels"
             xmlns:localization="clr-namespace:NETworkManager.Localization.Resources;assembly=NETworkManager.Localization"
             xmlns:settings="clr-namespace:NETworkManager.Settings;assembly=NETworkManager.Settings"
             xmlns:resources="clr-namespace:NETworkManager.Properties"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:internalControls="clr-namespace:NETworkManager.Controls"
             xmlns:profiles="clr-namespace:NETworkManager.Profiles;assembly=NETworkManager.Profiles"
             xmlns:wpfHelpers="clr-namespace:NETworkManager.Utilities.WPF;assembly=NETworkManager.Utilities.WPF"
             xmlns:networkManager="clr-namespace:NETworkManager"
             xmlns:hostControls="clr-namespace:NETworkManager.Views.Controls"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:WebConsoleHostViewModel}">
    <UserControl.Resources>
        <converters:BooleanToVisibilityCollapsedConverter x:Key="BooleanToVisibilityCollapsedConverter" />
        <converters:BooleanReverseToVisibilityCollapsedConverter x:Key="BooleanReverseToVisibilityCollapsedConverter" />
        <converters:IntNotZeroToVisibilityCollapsedConverter x:Key="IntNotZeroToVisibilityCollapsedConverter" />
        <converters:IntZeroToVisibilityCollapsedConverter x:Key="IntZeroToVisibilityCollapsedConverter" />
        <wpfHelpers:BindingProxy x:Key="BindingProxy" Data="{Binding}" />
        <DataTemplate x:Key="WebConsoleProfileItemTemplate" DataType="{x:Type profiles:ProfileInfo}">
            <Grid Background="Transparent">
                <Grid.ToolTip>
                    <ToolTip MinWidth="250">
                        <StackPanel Margin="10">
                            <TextBlock Text="{Binding WebConsole_Url}"
                                       Style="{StaticResource DefaultTextBlock}" />
                            <Separator Background="{DynamicResource MahApps.Brushes.Gray8}"
                                       Margin="0,10" />
                            <TextBlock Text="{Binding Description}"
                                       Style="{StaticResource InfoTextBlock}" TextWrapping="Wrap" />
                        </StackPanel>
                    </ToolTip>
                </Grid.ToolTip>
                <TextBlock Text="{Binding Name}" Style="{StaticResource CenterTextBlock}" />
            </Grid>
        </DataTemplate>
        <x:Array x:Key="WebConsoleProfileContextMenuExtraItems" Type="{x:Type MenuItem}">
            <MenuItem Header="{x:Static localization:Strings.Connect}"
                      Command="{Binding ConnectProfileCommand}">
                <MenuItem.Icon>
                    <Rectangle Width="16" Height="16"
                               Fill="{DynamicResource MahApps.Brushes.Gray3}">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Uniform"
                                         Visual="{iconPacks:Material Kind=Play}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </MenuItem.Icon>
            </MenuItem>
        </x:Array>
    </UserControl.Resources>
    <UserControl.InputBindings>
        <KeyBinding Key="T" Modifiers="Ctrl" Command="{Binding ConnectCommand}" />
    </UserControl.InputBindings>
    <Grid>
        <Grid>
            <Grid.Style>
                <Style TargetType="{x:Type Grid}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsRuntimeAvailable}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition MinWidth="{x:Static settings:GlobalStaticConfiguration.Profile_WidthCollapsed}"
                                  Width="{Binding Source={x:Static settings:ProfileViewState.Current}, Path=ProfileWidth, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  MaxWidth="{x:Static settings:GlobalStaticConfiguration.Profile_MaxWidthExpanded}" />
            </Grid.ColumnDefinitions>
            <dockablz:Layout Grid.Column="0" Grid.Row="0"
                             Partition="{Binding InterTabPartition}">
                <dockablz:Layout.Resources>
                    <DataTemplate x:Key="TabHeaderTemplate" DataType="{x:Type internalControls:DragablzTabItem}">
                        <Border BorderBrush="{DynamicResource MahApps.Brushes.Gray8}" BorderThickness="0,0,1,0">
                            <Grid Height="32">
                                <Grid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="{x:Static localization:Strings.Reload}"
                                                  Command="{Binding Path=Data.(viewModels:WebConsoleHostViewModel.ReloadCommand), Source={StaticResource BindingProxy}}"
                                                  CommandParameter="{Binding View}">
                                            <MenuItem.Icon>
                                                <Rectangle Width="16" Height="16"
                                                           Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                                    <Rectangle.OpacityMask>
                                                        <VisualBrush Stretch="Uniform"
                                                                     Visual="{iconPacks:Material Kind=Refresh}" />
                                                    </Rectangle.OpacityMask>
                                                </Rectangle>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </Grid.ContextMenu>
                                <Grid.InputBindings>
                                    <MouseBinding Command="dragablz:TabablzControl.CloseItemCommand"
                                                  Gesture="MiddleClick" />
                                </Grid.InputBindings>
                                <StackPanel Orientation="Horizontal">
                                    <Grid ToolTip="{Binding Header}">
                                        <TextBlock Text="{Binding Header}" Width="150" Margin="10,0"
                                                   VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="{x:Type TextBlock}"
                                                       BasedOn="{StaticResource HeaderTextBlock}">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                                    <Style.Triggers>
                                                        <DataTrigger
                                                            Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type dragablz:DragablzItem}}, Path=IsMouseOver}"
                                                            Value="True">
                                                            <Setter Property="Foreground"
                                                                    Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                                        </DataTrigger>
                                                        <DataTrigger
                                                            Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type dragablz:DragablzItem}}, Path=IsSelected}"
                                                            Value="True">
                                                            <Setter Property="Foreground"
                                                                    Value="{DynamicResource MahApps.Brushes.Accent}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <Thumb Style="{DynamicResource InvisibleThumbStyle}"
                                               dragablz:DragablzItem.IsCustomThumb="True" />
                                    </Grid>
                                    <Button Style="{StaticResource CleanButton}"
                                            Command="dragablz:TabablzControl.CloseItemCommand"
                                            ToolTip="{x:Static localization:Strings.Close}" Margin="0,0,10,0">
                                        <Rectangle Width="16" Height="16">
                                            <Rectangle.OpacityMask>
                                                <VisualBrush Stretch="Uniform"
                                                             Visual="{iconPacks:Material Kind=WindowClose}" />
                                            </Rectangle.OpacityMask>
                                            <Rectangle.Style>
                                                <Style TargetType="{x:Type Rectangle}">
                                                    <Setter Property="Fill"
                                                            Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                                    <Style.Triggers>
                                                        <DataTrigger
                                                            Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                            Value="True">
                                                            <Setter Property="Fill" Value="Red" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Rectangle.Style>
                                        </Rectangle>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </dockablz:Layout.Resources>
                <dockablz:Layout.BranchTemplate>
                    <DataTemplate>
                        <dragablz:TabablzControl DataContext="{Binding}"
                                                 HeaderItemTemplate="{StaticResource TabHeaderTemplate}"
                                                 ClosingItemCallback="{Binding CloseItemCommand}">
                            <dragablz:TabablzControl.InterTabController>
                                <dragablz:InterTabController DataContext="{Binding}"
                                                             InterTabClient="{Binding InterTabClient}"
                                                             Partition="{Binding InterTabPartition}" />
                            </dragablz:TabablzControl.InterTabController>
                        </dragablz:TabablzControl>
                    </DataTemplate>
                </dockablz:Layout.BranchTemplate>
                <dragablz:TabablzControl HeaderItemTemplate="{StaticResource TabHeaderTemplate}"
                                         ClosingItemCallback="{Binding CloseItemCommand}"
                                         ItemsSource="{Binding TabItems}"
                                         SelectedIndex="{Binding SelectedTabIndex}"
                                         DisableBranchConsolidation="True">
                    <dragablz:TabablzControl.InterTabController>
                        <dragablz:InterTabController InterTabClient="{Binding InterTabClient}"
                                                     Partition="{Binding InterTabPartition}"
                                                     MoveWindowWithSolitaryTabs="False" />
                    </dragablz:TabablzControl.InterTabController>
                    <dragablz:TabablzControl.HeaderSuffixContent>
                        <Grid HorizontalAlignment="Right" Width="32" Height="32">
                            <Button Style="{StaticResource CleanButton}"
                                    Command="{Binding ConnectCommand}"
                                    ToolTip="{x:Static localization:Strings.NewConnectionDots}"
                                    Focusable="False">
                                <Rectangle Width="16" Height="16">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Fill" Visual="{iconPacks:Material Kind=Plus}" />
                                    </Rectangle.OpacityMask>
                                    <Rectangle.Style>
                                        <Style TargetType="{x:Type Rectangle}">
                                            <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                            <Style.Triggers>
                                                <DataTrigger
                                                    Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                    Value="True">
                                                    <Setter Property="Fill"
                                                            Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </Button>
                        </Grid>
                    </dragablz:TabablzControl.HeaderSuffixContent>
                </dragablz:TabablzControl>
            </dockablz:Layout>
            <Grid Grid.Column="0" Grid.Row="0"
                  VerticalAlignment="Center"
                  Visibility="{Binding Source={x:Static settings:ConfigurationManager.Current}, Path=WebConsoleTabCount, Converter={StaticResource IntZeroToVisibilityCollapsedConverter}}">
                <Button HorizontalAlignment="Center" Command="{Binding ConnectCommand}"
                        Style="{StaticResource CleanButton}">
                    <Button.Content>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Rectangle Width="20" Height="20">
                                <Rectangle.OpacityMask>
                                    <VisualBrush Stretch="Fill" Visual="{iconPacks:Material Kind=Plus}" />
                                </Rectangle.OpacityMask>
                                <Rectangle.Style>
                                    <Style TargetType="{x:Type Rectangle}">
                                        <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                        <Style.Triggers>
                                            <DataTrigger
                                                Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Rectangle.Style>
                            </Rectangle>
                            <TextBlock Grid.Column="1" Text="{x:Static localization:Strings.NewConnectionDots}">
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MessageTextBlock}">
                                        <Style.Triggers>
                                            <DataTrigger
                                                Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}"
                                                Value="True">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Button.Content>
                </Button>
            </Grid>
            <GridSplitter Grid.Column="1" Grid.Row="0"
                          Focusable="False"
                          Style="{StaticResource VerticalGridSplitter}" />
            <hostControls:ProfileExpanderPanel Grid.Column="2" Grid.Row="0"
                                                ProfileItemTemplate="{StaticResource ResourceKey=WebConsoleProfileItemTemplate}"
                                                ProfileContextMenuExtraItems="{StaticResource ResourceKey=WebConsoleProfileContextMenuExtraItems}"
                                                RunProfileCommand="{Binding Path=ConnectProfileCommand}" />
        </Grid>
        <StackPanel VerticalAlignment="Center" Margin="10">
            <StackPanel.Style>
                <Style TargetType="{x:Type StackPanel}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsRuntimeAvailable}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="{x:Static localization:Strings.MicrosoftEdgeWebView2RuntimeIsNotAvailable}"
                       Style="{StaticResource MessageTextBlock}" Margin="0,0,0,10" />
            <TextBlock Text="{x:Static localization:Strings.DownloadMicrosoftEdgeWebView2Runtime}"
                       Style="{StaticResource LinkTextBlock}" Margin="0,0,0,5" HorizontalAlignment="Center">
                <TextBlock.InputBindings>
                    <MouseBinding Command="{Binding OpenWebsiteCommand}"
                                  CommandParameter="{x:Static resources:Resources.MicrosoftEdgeWebView2Runtime_DownloadUrl}"
                                  MouseAction="LeftClick" />
                </TextBlock.InputBindings>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>