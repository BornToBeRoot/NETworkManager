<UserControl x:Class="NETworkManager.Views.SpeedTestWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:converters="clr-namespace:NETworkManager.Converters;assembly=NETworkManager.Converters"
             xmlns:localization="clr-namespace:NETworkManager.Localization.Resources;assembly=NETworkManager.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:NETworkManager.ViewModels"
             mc:Ignorable="d" MinHeight="200"
             d:DataContext="{d:DesignInstance viewModels:SpeedTestWidgetViewModel}">
    <UserControl.Resources>
        <converters:BooleanReverseConverter x:Key="BooleanReverseConverter" />
        <converters:BooleanReverseToVisibilityCollapsedConverter x:Key="BooleanReverseToVisibilityCollapsedConverter" />
        <converters:BooleanToVisibilityCollapsedConverter x:Key="BooleanToVisibilityCollapsedConverter" />
        <converters:NullableDoubleToStringConverter x:Key="NullableDoubleToStringConverter" />
    </UserControl.Resources>
    <Border BorderThickness="1" BorderBrush="{DynamicResource MahApps.Brushes.Gray8}">
        <Grid Margin="10">
            <Grid.Resources>
                <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CenterTextBlock}" />
                <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBlockAsTextBox}">
                    <Setter Property="ContextMenu" Value="{StaticResource CopyContextMenu}" />
                    <Setter Property="TextWrapping" Value="NoWrap" />
                </Style>
            </Grid.Resources>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />                    
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <!-- Header: icon + title (left), status + action button (right) -->
                <Grid Grid.Row="0" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Rectangle Width="32" Height="32" Fill="{DynamicResource MahApps.Brushes.Gray3}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Uniform" Visual="{iconPacks:Material Kind=Speedometer}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <TextBlock Text="{x:Static localization:Strings.SpeedTest}"
                                   Style="{StaticResource MessageTextBlock}" 
                                   Margin="10,0,0,0" />
                    </StackPanel>
                    <!-- Status (running): spinner + phase -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"
                                Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}">
                        <mah:ProgressRing Width="16" Height="16" IsActive="True"
                                          Foreground="{DynamicResource MahApps.Brushes.Accent}" />
                        <TextBlock Text="{Binding StatusMessage}"
                                   Style="{StaticResource AccentTextBlock}"
                                   Margin="10,0,0,0"                                   
                                   VerticalAlignment="Center" />
                    </StackPanel>
                    <!-- Action button (icon-only) -->
                    <Button Grid.Column="4" VerticalAlignment="Center"
                            Command="{Binding RunCommand}"
                            Style="{StaticResource CleanButton}"
                            Visibility="{Binding ShowDisclaimer, Converter={StaticResource BooleanReverseToVisibilityCollapsedConverter}}"
                            IsDefault="{Binding IsRunning, Converter={StaticResource BooleanReverseConverter}}"
                            IsCancel="{Binding IsRunning}">
                        <Button.ToolTip>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="Text" Value="{x:Static localization:Strings.RunSpeedTest}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                <Setter Property="Text" Value="{x:Static localization:Strings.Stop}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button.ToolTip>
                        <Rectangle Width="20" Height="20">
                            <Rectangle.Resources>
                                <VisualBrush x:Key="VisualPlay" Stretch="Uniform"
                                             Visual="{iconPacks:Material Kind=Play}" />
                                <VisualBrush x:Key="VisualClose" Stretch="Uniform"
                                             Visual="{iconPacks:Material Kind=Close}" />
                            </Rectangle.Resources>
                            <Rectangle.Style>
                                <Style TargetType="{x:Type Rectangle}">
                                    <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray3}" />
                                    <Setter Property="OpacityMask" Value="{StaticResource VisualPlay}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="OpacityMask" Value="{StaticResource VisualClose}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Gray5}" />
                                        </DataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsRunning}" Value="True" />
                                                <Condition Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Button}}, Path=IsMouseOver}" Value="True" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Fill" Value="Red" />
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Rectangle.Style>
                        </Rectangle>
                    </Button>
                </Grid>
                <!-- Body -->
                <Grid Grid.Row="1" Margin="0">
                    <!-- Disclaimer -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                Visibility="{Binding ShowDisclaimer, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}">
                        <TextBlock Text="{x:Static localization:Strings.SpeedTestDisclaimerMessage}"
                                   Style="{StaticResource MessageTextBlock}" Margin="0,0,0,10" />
                        <Button Command="{Binding AcceptDisclaimerCommand}" HorizontalAlignment="Center">
                            <Button.Resources>
                                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource ImageWithTextButton}" />
                            </Button.Resources>
                            <Button.Content>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Rectangle Grid.Column="0" Grid.Row="0"
                                               Style="{StaticResource ResourceKey=ButtonWithImageRectangle}">
                                        <Rectangle.OpacityMask>
                                            <VisualBrush Stretch="Uniform"
                                                         Visual="{iconPacks:Material Kind=Play}" />
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                    <TextBlock Grid.Column="1"
                                               Style="{StaticResource ButtonWithImageTextBlock}"
                                               Text="{x:Static localization:Strings.SpeedTest}" />
                                </Grid>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                    <!-- Error panel (same level as disclaimer — replaces data view entirely) -->
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel.Style>
                            <Style TargetType="{x:Type StackPanel}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsRunning}" Value="False" />
                                            <Condition Binding="{Binding Result.HasError}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="{Binding Result.ErrorMessage}"
                                   Style="{StaticResource MessageTextBlock}" Margin="0,0,0,10" />
                        <TextBlock Text="{x:Static localization:Strings.CheckNetworkConnectionTryAgainMessage}"
                                   Style="{StaticResource MessageTextBlock}" />
                    </StackPanel>
                    <!-- Test view: three equal columns (metadata / download / upload) -->
                    <!-- Hidden when disclaimer is shown or when there is an error -->
                    <Grid>
                        <Grid.Style>
                            <Style TargetType="{x:Type Grid}">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowDisclaimer}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsRunning}" Value="False" />
                                            <Condition Binding="{Binding Result.HasError}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="3*" />
                        </Grid.ColumnDefinitions>
                        <!-- Col 0: Metadata (ISP / Location / Jitter / Latency) -->
                        <Grid Grid.Column="0" VerticalAlignment="Center" Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="5" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="5" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="5" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Column="0" Grid.Row="0"
                                       Text="{x:Static localization:Strings.ISP}" />
                            <TextBox Grid.Column="2" Grid.Row="0"
                                     ToolTip="{Binding Result.Isp}"
                                     Text="{Binding Result.Isp, TargetNullValue=-/-, FallbackValue=-/-, Mode=OneWay}" />
                            <TextBlock Grid.Column="0" Grid.Row="2"
                                       Text="{x:Static localization:Strings.Server}" />
                            <TextBox Grid.Column="2" Grid.Row="2">
                                <TextBox.Text>
                                    <MultiBinding StringFormat="{}{0}, {1} ({2})" Mode="OneWay">
                                        <Binding Path="Result.ServerCity" TargetNullValue="-/-" />
                                        <Binding Path="Result.ServerCountry" TargetNullValue="-/-" />
                                        <Binding Path="Result.ServerIata" TargetNullValue="-/-" />
                                    </MultiBinding>
                                </TextBox.Text>
                            </TextBox>
                            <TextBlock Grid.Column="0" Grid.Row="4"
                                       Text="{x:Static localization:Strings.Latency}" />
                            <TextBox Grid.Column="2" Grid.Row="4"
                                     Text="{Binding CurrentLatencyMs, Converter={StaticResource NullableDoubleToStringConverter}, ConverterParameter='F0|ms', Mode=OneWay}" />
                            <TextBlock Grid.Column="0" Grid.Row="6"
                                       Text="{x:Static localization:Strings.Jitter}" />
                            <TextBox Grid.Column="2" Grid.Row="6"
                                     Text="{Binding CurrentJitterMs, Converter={StaticResource NullableDoubleToStringConverter}, ConverterParameter='F1|ms', Mode=OneWay}" />
                        </Grid>
                        <!-- Col 2: Download (label left, value right, chart below) -->
                        <StackPanel Grid.Column="2">
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                                <Rectangle Width="24" Height="24" Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform" Visual="{iconPacks:Material Kind=CloudDownloadOutline}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Text="{Binding CurrentDownloadMbps, Converter={StaticResource NullableDoubleToStringConverter}, ConverterParameter='F1|Mbps'}"
                                           Style="{StaticResource MessageTextBlock}"
                                           Margin="10,0,0,0" />
                            </StackPanel>
                            <lvc:CartesianChart Height="100"
                                                Background="Transparent"
                                                Series="{Binding DownloadSeries}"
                                                XAxes="{Binding DownloadXAxes}"
                                                YAxes="{Binding DownloadYAxes}"
                                                DrawMarginFrame="{x:Null}" />
                        </StackPanel>
                        <!-- Col 4: Upload (label left, value right, chart below) -->
                        <StackPanel Grid.Column="4">
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                                <Rectangle Width="24" Height="24" Fill="{DynamicResource MahApps.Brushes.Gray3}">
                                    <Rectangle.OpacityMask>
                                        <VisualBrush Stretch="Uniform" Visual="{iconPacks:Material Kind=CloudUploadOutline}" />
                                    </Rectangle.OpacityMask>
                                </Rectangle>
                                <TextBlock Text="{Binding CurrentUploadMbps, Converter={StaticResource NullableDoubleToStringConverter}, ConverterParameter='F1|Mbps'}"
                                           Style="{StaticResource MessageTextBlock}"
                                           Margin="10,0,0,0" />
                            </StackPanel>
                            <lvc:CartesianChart Height="100"
                                                Background="Transparent"
                                                Series="{Binding UploadSeries}"
                                                XAxes="{Binding UploadXAxes}"
                                                YAxes="{Binding UploadYAxes}"
                                                DrawMarginFrame="{x:Null}" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
