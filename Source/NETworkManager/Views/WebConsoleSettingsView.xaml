<UserControl x:Class="NETworkManager.Views.WebConsoleSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:viewModels="clr-namespace:NETworkManager.ViewModels"
             xmlns:localization="clr-namespace:NETworkManager.Localization.Resources;assembly=NETworkManager.Localization"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:WebConsoleSettingsViewModel}">
    <StackPanel>
        <TextBlock Text="{x:Static localization:Strings.WebConsole}" Style="{StaticResource HeaderTextBlock}" />
        <mah:ToggleSwitch Header="{x:Static localization:Strings.ShowAddressBar}"
                          IsOn="{Binding ShowAddressBar}" Margin="0,0,0,10" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <mah:ToggleSwitch Header="{x:Static localization:Strings.ShowStatusBar}" 
                              IsOn="{Binding IsStatusBarEnabled}" />
            <Rectangle Width="24" Height="24" VerticalAlignment="Top"
                       ToolTip="{x:Static localization:Strings.HelpMessage_ShowStatusBar}"
                       Style="{StaticResource HelpImageRectangle}" Margin="10,0,0,0">
                <Rectangle.Resources>
                    <Style TargetType="{x:Type ToolTip}" BasedOn="{StaticResource HelpToolTip}" />
                </Rectangle.Resources>
            </Rectangle>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <mah:ToggleSwitch Header="{x:Static localization:Strings.ShowOptionToSavePasswords}" 
                              IsOn="{Binding IsPasswordSaveEnabled}" />
            <Rectangle Width="24" Height="24" VerticalAlignment="Top"
                       ToolTip="{x:Static localization:Strings.HelpMessage_ShowOptionToSavePasswords}"
                       Style="{StaticResource HelpImageRectangle}" Margin="10,0,0,0">
                <Rectangle.Resources>
                    <Style TargetType="{x:Type ToolTip}" BasedOn="{StaticResource HelpToolTip}" />
                </Rectangle.Resources>
            </Rectangle>
        </StackPanel>
        <Button Content="{x:Static localization:Strings.ClearBrowsingData}" Command="{Binding Path=ClearBrowsingDataCommand}"
                Style="{StaticResource DefaultButton}" HorizontalAlignment="Left"
                Margin="0,0,0,10" />
    </StackPanel>
</UserControl>
