<UserControl x:Class="NETworkManager.Controls.LiveChartsWiFiChannelTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Border Background="{DynamicResource MahApps.Brushes.Window.Background}"
            BorderBrush="{DynamicResource MahApps.Brushes.Gray8}"
            BorderThickness="1"
            IsHitTestVisible="False">
        <ItemsControl Margin="10" ItemsSource="{Binding TooltipEntries}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Rectangle Grid.Column="0" Height="2" Width="14"
                                   VerticalAlignment="Center"
                                   Fill="{Binding SeriesColor}" />
                        <TextBlock Grid.Column="2" Text="{Binding Ssid}"
                                   Style="{StaticResource AccentTextBlock}"
                                   VerticalAlignment="Center" />
                        <TextBlock Grid.Column="4" Text="{Binding Detail}"
                                   Style="{StaticResource DefaultTextBlock}"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource MahApps.Brushes.Gray3}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
