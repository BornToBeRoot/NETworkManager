# Search/Filter -> When nothing found -> "Application not found!"
# RestartAdAdministrator -> Restart -> Change view to config after restart



LEGENDE

Variable beginnt mit Großbuchstaben		->		Public
Variable beginnt mit _					->		Private innerhalb einer Klasse
Variable beginnt mit Kleinbuchstabe		->		Private innerhalb ein eines Voids/Subroutine


--- Ping stats backup

// // foreach(string s in HostnameOrIPAddressHistory) { MessageBox.Show(s); }

            //byte[] buffer = new byte[Buffer];

            //do
            //{

            //    PingInfo pingInfo = await Model.Network.Ping.SendAsync(ipAddress);

            //    PingResults.Add(pingInfo);

            //    // Some statistics
            //    if (pingInfo.Status == IPStatus.Success)
            //    {
            //        // Some statistics...
            //        if (PingsTransmitted == 1)
            //        {
            //            MinimumTime = pingInfo.Time;
            //            MaximumTime = pingInfo.Time;
            //        }
            //        else
            //        {
            //            if (MinimumTime > pingInfo.Time)
            //                MinimumTime = pingInfo.Time;

            //            if (MaximumTime < pingInfo.Time)
            //                MaximumTime = pingInfo.Time;
            //        }

            //        PingsReceived++;

            //        // I just hope this don't slow on large collections
            //        AverageTime = (int)PingResults.Average(s => s.Time);
            //    }
            //    else
            //    {
            //        PingsLost++;
            //    }

            //    PingsTransmitted++;

            //    // If ping is canceled... dont wait for example 5 seconds
            //    for (int i = 0; i < WaitTime; i += 250)
            //    {
            //        await Task.Delay(250);

            //        if (CancelPing)
            //            break;
            //    }

            //} while (!CancelPing && (Attempts == 0 || PingsTransmitted < Attempts));